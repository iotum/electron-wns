{
  "name": "electron-wns",
  "version": "0.0.8",
  "description": "Native Node addon for WNS channel creation and foreground notification listening",
  "main": "index.js",
  "types": "types.d.ts",
  "scripts": {
    "clean": "node-gyp clean",
    "build": "node-gyp configure && node-gyp build",
    "postinstall": "node scripts/auto-rebuild.js"
  },
  "keywords": [
    "electron",
    "wns",
    "windows",
    "native-addon"
  ],
  "license": "MIT",
  "gypfile": true,
  "dependencies": {
    "@electron/rebuild": "^3.7.1",
    "bindings": "^1.5.0",
    "node-addon-api": "^8.1.0"
  },
  "devDependencies": {
    "electron-rebuild": "^3.2.9",
    "node-gyp": "^11.1.0"
  }
}
